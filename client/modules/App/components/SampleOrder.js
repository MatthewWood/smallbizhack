import React from 'react';

export function SampleOrder(props) {
  return (
    <div>
      "Hello!"
    </div>
  );
}

// Functions which are needed, this is also needed
SampleOrder.need = [params => {[]}];

export default SampleOrder;
